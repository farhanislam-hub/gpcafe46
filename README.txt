GP CAFE 46 - GITHUB FIXED VERSION

Upload these files DIRECTLY into the ROOT of your GitHub repository:
- index.html
- style.css
- script.js
- README.txt

The generated concept image is embedded directly inside style.css, so the assets folder is not needed for that image.

GitHub Pages: Settings > Pages > Deploy from a branch > main > / (root) > Save.
Wait 1-3 minutes, then refresh with Ctrl+F5.

Food/drink card photos use online image URLs, so internet is required for those photos.
